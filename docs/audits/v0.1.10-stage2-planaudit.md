# v0.1.10 Stage 2 PLANNING Opus PlanAudit verdict

**Date**: 2026-05-08
**Reviewer**: Opus general-purpose (single dispatch per `auto-review-cadence.md §Planning Cadence` + `stage-runbook.md §PLAN_AUDIT` candidate-set mode)
**Verdict**: APPROVED_WITH_REVISIONS
**Token estimate**: ~14 kT

## Recommended candidates

| Sub-task | Recommended | Rationale |
|---|---|---|
| **T-2-2** GHA real-claude | **A-then-B-with-pre-validation** | 5-min throwaway GHA workflow (install+version+non-interactive smoke) BEFORE main measurement. PASS → A; FAIL → CCBL-Stage2-T22-stub-fallback + B. Bail-criterion: smoke exit !=0 OR stdout matches `/authenticate\|login required/i` OR install >4min. Pre-val cost ~5 kT amortizes vs ~30 kT mid-measurement bail. |
| **T-2-4** §reliability budget | **A** (additive §7 term (f)) | PO preference confirmed; §7 term-additive pattern (a)-(e) is established v0.1.8 amendment precedent; B (new §8) orphans cross-references. |

## Critical decisions

- **N5_vs_N10_protocol_deviation**: `ccb_light_required` per `feedback_silent-deviation-prohibited.md`. AC-1 in measurement-protocol.v0.1.9.md/v0.1.10.md is N≥10 inherited normative requirement. RD's "charter explicit overrides protocol" is the precise pattern that lesson prohibits. **TeamLead resolution (Auto Mode)**: bump N=5 → N=10 to honor inherited AC-1 — no CCB-Light deviation needed; cost +50% on T-2-1+T-2-2 measurement budget (~+15-20 kT total). Avoids registering a deviation that would compromise statistical validity at Gate_Requirement.
- **AC7_dual_host_charter_vs_same_host_constraint**: `consistent`. Charter Q3 "real-claude 驗證限本機同一 host (跨機型留 v0.1.11)" = "each measurement self-contained on a single host without cross-host transfer mid-measurement", not "only one host total". AC-7 dual-host is two reference environments. Reconciliation must be stated explicitly in T-2-3 §interpretation.
- **AC7_partial_path_acceptability**: `partial_acceptable_for_Stage2_close` IF empirical pre-validation evidence proves GHA real-claude unavailable. Direct-to-B without pre-validation = self-skip → Gate_Requirement should FAIL.

## Blockers (revisions required before EXECUTING)

1. **T-2-1 N=5 deviation** — silent scope-pruning of inherited AC-1 N≥10. **Resolution**: bump to N=10 (TeamLead Auto Mode decision); no CCBL needed.
2. **T-2-2 RAID-I-Stage2-GHA-claude unmitigated** — runtime A→B bail wastes budget. **Resolution**: insert pre-validation step + explicit bail-criterion before main measurement. Apply autonomously.

## Major

3. **T-2-4 anchor mismatch** — Charter AC-8 says `§reliability budget` but design.md doesn't have that anchor. PO Plan A creates §7 term (f). **Resolution**: register CCBL-Stage2-T24-anchor charter-clarification: "AC-8 §reliability budget anchor pragmatically realized as §7 term (f) per v0.1.10 PlanAudit; semantic equivalence preserved; no FROZEN-spec violation."

## Minor (apply at EXECUTING)

- T-2-1: use inherited v0.1.9 §statistics-computation method for p50/p95/max at N=10 (no degeneracy)
- T-2-1: 100 launchctl ask-prompts under D-3 → CEO_Gate fatigue risk; document per-invocation approval as accepted UX cost
- T-2-3: pre-declare `AC closures evidenced: AC-7` in §metadata; expand §methodology-deviations-inheritance to enumerate v0.1.9 deviation-1, deviation-2 status
- T-2-5: add failure_message: `python3 version <3.10 detected; AC-9 hard gate FAIL — ensure setup-python@v5 input python-version: 3.10+ precedes this step`

## Anti-self-skip check

- `deferred_to_teamlead`: false
- `explicit_candidate_selected`: true
- `evidence`: T-2-2 hybrid is explicit deterministic-bail strategy not "try and see"; T-2-4 A confirmed with structural rationale; N=5 blocker flagged with concrete CCBL row prescription pre-EXECUTING.
